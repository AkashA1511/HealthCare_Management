const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("MedicalRecord" ,()=>{
    let user1,medical,transactionResponse,transactionRecipet;
    beforeEach(async () => {
        const accounts = await ethers.getSigner();
    })

})